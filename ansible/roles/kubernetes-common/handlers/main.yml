---
- name: Reload sysctl
  ansible.builtin.command:
    argv:
      - sysctl
      - --system
  changed_when: true

- name: Restart kubelet
  ansible.builtin.service:
    name: kubelet
    state: restarted
  failed_when: false
